from django.apps import AppConfig


class QuizConfig(AppConfig):
    """
    The application registry maintains metadata in an AppConfig instance
    for each installed application.
    """
    name = 'quiz'
